---
# inventory.yml
# Inventário em formato YAML (compatível com o tipo "Static YAML" do SemaphoreUI).

all:
  children:
    wazuh_managers:
      hosts:
        wazuh-manager-01:
          ansible_host: 10.0.0.10
          ansible_port: 22
          ansible_user: admin
